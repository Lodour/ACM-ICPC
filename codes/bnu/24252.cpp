#include <iostream>
#include <cstdio>
#include <algorithm>
using namespace std;

int main() {
#ifdef MANGOGAO
	freopen("data.in", "r", stdin);
#endif

	int T;
	scanf("%d", &T);
	for (int t = 1; t <= T; t++) {
		
		printf("Case #%d:\n", t);
	}

	return 0;
}
